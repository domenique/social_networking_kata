group 'io.tripled.socialnetwork'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.9

javaModule.name = 'presentation'

repositories {
    mavenCentral()
}

dependencies {
    compile project(':application')
    compile 'junit:junit:4.11'
}
